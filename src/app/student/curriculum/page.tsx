/**
 * Curriculum (module cards)
 *
 * STATUS: scaffold — implement against docs/02-exam-spec.md and the
 * approved design in docs/design/.
 */
export default function Page() {
  return <main style={{ padding: 40 }}>Curriculum (module cards) — coming soon</main>;
}
