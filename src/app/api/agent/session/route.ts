/**
 * POST /api/agent/session — agent reports login/logout on a PC; opens/closes Session rows
 * STATUS: scaffold — implement using lib/exam.ts. All exam rules live there.
 */
export async function POST() {
  return Response.json({ error: "not implemented" }, { status: 501 });
}
