# Wall display (projector)

Read-only live pulse grid shown on the classroom projector.
Planned as a single route in the main app (`/wall`) opened full-screen
on the projector PC — no separate codebase needed.

Comes AFTER exams + attendance + settings are done.
